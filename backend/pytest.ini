[pytest]
pythonpath = .
asyncio_mode = auto
# Only tests/: eval_data/ holds throwaway experiment scripts (some named *_test.py)
# that read recordings and models, and must never be collected as tests.
testpaths = tests
